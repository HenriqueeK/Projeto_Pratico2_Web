CREATE TABLE autor (
    id_autor SERIAL PRIMARY KEY,
    nome VARCHAR(200),
    nacionalidade VARCHAR(200),
    dt_nascimento DATE
);

CREATE TABLE livro (
    id_livro SERIAL PRIMARY KEY,
    titulo VARCHAR(200),
    autor VARCHAR(200), -- Não fiz como estrangeira pois não sabia como comunicar certo no php
    genero VARCHAR(200),
    n_paginas INT
);

ALTER TABLE autor ADD COLUMN cep VARCHAR(9);
ALTER TABLE autor ADD COLUMN cidade VARCHAR(100);
ALTER TABLE autor ADD COLUMN estado VARCHAR(2);